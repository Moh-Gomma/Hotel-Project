@model IEnumerable<Villa>
@{
    ViewData["title"] = "Villa List ";
}

<div class="mb-4">
    <h3 class="text-center"> List Of Villa </h3>
    <a asp-action="Create" asp-controller="Villa" class="btn btn-secondary">
        <i class="bi bi-patch-plus"></i>
        New Villa
    </a>
</div>


<table class="table table-striped table-bordered table-hover">

    <thead>
        <tr class="text-center">
            <th scope="col">Name</th>
            <th scope="col">Price</th>
            <th scope="col">Sqft</th>
            <th scope="col">Ocuupancy</th>
            <th scope="col">Action</th>
        </tr>
    </thead>
    <tbody class="table-group-divider">
        @foreach (var obj in Model)
        {
            <tr class="text-center ">
                <td>@obj.Name</td>
                <td>@obj.Price</td>
                <td>@obj.Sqft</td>
                <td>@obj.Occupancy</td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-action="Update" asp-controller="Villa" asp-route-id="@obj.Id"
                           class="btn btn-info btn-sm mx-2">
                           <i class="bi bi-pencil-square"></i>
                            Update
                        </a>

                        <a asp-action="Delete" asp-controller="Villa" asp-route-id="@obj.Id"
                           class="btn btn-danger btn-sm mx-2">
                            <i class="bi bi-trash-fill"></i>
                            Delete
                        </a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>